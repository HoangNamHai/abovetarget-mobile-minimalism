import { Text, View } from 'react-native';

/**
 * Placeholder root index — Task 9 will replace this with the real (tabs) group.
 */
export default function Index() {
  return (
    <View style={{ flex: 1, alignItems: 'center', justifyContent: 'center' }}>
      <Text>Loading…</Text>
    </View>
  );
}
